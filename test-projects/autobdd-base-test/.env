ABDD_PROJECT=autobdd-base-test
# Default to the locally built dev tag; override with AutoBDD_Ver=<v> for a release.
AutoBDD_Ver=dev
# USER/HOSTOS/USERID/GROUPID/PASSWORD/VNC_PASSWORD are exported from the shell by
# `make` (see Makefile) or your environment. Keep them out of .env because
# docker-compose does not perform shell substitution here.
